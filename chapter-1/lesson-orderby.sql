SELECT * FROM customers ORDER BY first_name DESC;


SELECT customer_id, first_name, last_name FROM customers ORDER BY first_name ASC, last_name DESC;


SELECT customer_id, first_name, last_name FROM customers ORDER BY 1, 2;